# Logging Conventions

- Log enough to debug real failures.
- Prefer exact entity identifiers and route names.
- Avoid noisy logs without diagnostic value.
- Improve logging before guessing when root cause is unclear.
